<UserControl x:Class="DesignPatternsWpf.Views.DesignPatternView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:DesignPatternsWpf.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:DesignPatternViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TreeView ItemsSource="{Binding Patterns}" DisplayMemberPath="Header"/>
        <!--<ListView ItemsSource="{Binding Patterns}"  SelectedItem="{Binding SelectedPattern}">
            <ListView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Patterns}">
                    <TextBlock Text="{Binding Header}"/>
                </HierarchicalDataTemplate>
            </ListView.ItemTemplate>  
        </ListView>-->
        <TextBox Grid.Row="1" Background="AliceBlue" Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>
